// // app/lib/reportsStore.ts
// import type { BasicReportForm } from "@/app/page.tsx";

// // Super tiny in-memory store.
// // Keys = report ID (string)
// // Value = the report object.
// const reports: Record<string, BasicReportForm> = {};

// export function saveReport(id: string, data: BasicReportForm) {
//   reports[id] = data;
// }

// export function getReport(id: string) {
//   return reports[id] || null;
// }
